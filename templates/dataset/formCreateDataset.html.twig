{% extends 'Assets/popUp.html.twig' %}

{% block form_start %}
	{# <p>info</p> #}
	{# {{ form_start(dataset_form) }} #}
	<form method="post" action="{{ path('app_dataset') }}"> {% endblock %}

	{% block body %}
		<div class="row">
			<div class="d-flex bd-highlight">
				<div class="flex-grow-1 bd-highlight">
					<h1 class="text-center pb-3">Add Dataset</h1>

					{{ form_row(dataset_form.title) }}
					{{ form_row(dataset_form.link_api) }}
					{{ form_row(dataset_form.description) }}

				</div>
				{# <div class="p-2 bd-highlight w-50"></div> #}
			</div>
		</div>
	{% endblock %}

	{% block form_button %}
		<button type="submit" class="btn btn-primary">add</button>
	{% endblock %}

	{% block form_end %}
		{{ form_end(dataset_form) }}
	{% endblock %}
